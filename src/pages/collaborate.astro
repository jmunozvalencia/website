---
// Component Imports
import MainHead from '../components/MainHead.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';
		import Features2 from '../components/Widges/Features2.astro';

// Data Fetching: List all Markdown posts in the repo.
const projects = await Astro.glob('./project/**/*.md');
const featuredProject = projects[0];

// Full Astro Component Syntax:
// https://docs.astro.build/core-concepts/astro-components/
---

<html lang="en">
	<head>
		<MainHead title="Colabora | Art Oasis" description="" />
		<head>
			<style>
				.heroImg {
					max-height: 24rem;
					object-fit: cover;
					overflow: hidden;
				}
	
				.heroImg img {
					width: 100%;
				}
	
				.bio {
					font-size: var(--f-u1);
					line-height: 2;
					margin-top: 4em;
				}
	
				.wrapper.bio {
					max-width: 50em;
				}
			</style>
		</head>
	</head>
	<body>
		<Nav />
		<div class="wrapper">
			<h1>Colabora</h1>
			<div class="heroImg">
				<img
					width="1400"
					height="350"
					src="/colabora.png"
				/>
			</div>
			<p class="phrase">
				Hasta el más largo de los viajes se inicia con un primer y solitario paso. <br>Un paso hacia el continente africano, donde se escucha más el “nosotros” que el “yo”.
			</p>
			<div class="bio wrapper">
				<Features2 />
			</div>
		</div>
		<Footer />
	</body>
</html>

<style>
	h1 { margin: 0}
	pre {
		margin-top: 4rem;
		white-space: pre-line;
	}
	lite-youtube {
		margin-left: auto;
		margin-right: auto;
		width: 720px;
	}

	box {
		padding: 30px;
		border-radius: 20px;
		border: 2px solid #00aa96;
		text-align: end;
		font-style: italic;
		margin-top: 50px;
		background-color: white;
	}
	.phrase {
		color: #00aa96;
		font-style: italic;
		margin: 30px 0;
		text-align: end;
		line-height: 2;
		font-size: large;
	}
</style>
